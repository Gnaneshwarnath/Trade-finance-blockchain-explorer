# ChainDocs Backend

## Setup

1. Create PostgreSQL DB: chaindocs
2. Update .env credentials
3. Install dependencies:
   pip install -r requirements.txt
4. Run server:
   uvicorn app.main:app --reload

Visit:
http://127.0.0.1:8000/docs